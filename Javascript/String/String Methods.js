let a = "Shreyank Maurya";
let b = "Hello Hello Hello";

// Trim
console.log(a.trim());

// toUpperCase
console.log(a.toUpperCase());

// toLowerCase
console.log(a.toLowerCase());

// indexOf
console.log(a.indexOf(a));

// slice(start,end)
console.log(a.slice(4, 8));

// slice(start)
console.log(a.slice(4));

// slice(-ve n)
console.log(a.slice(-4));

// replace
console.log(a.replace("Maurya", ""));

// replaceAll
console.log(a.replaceAll("Hello", "Hii"));

// repeat
console.log(a.repeat(4));
